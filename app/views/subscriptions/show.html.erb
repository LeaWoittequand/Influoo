<div class="container">
  <div class="row">
    <h1>Premium Subscription</h1>
    <p>Amount: <%= humanized_money_with_symbol(@subscription.price) %></p>

    <%= form_tag orders_path do %>
  <%= hidden_field_tag 'subscription_id', @subscription.id %>
  <%= submit_tag 'Purchase', class: 'btn btn-primary' %>
<% end %>

  </div>
</div>
